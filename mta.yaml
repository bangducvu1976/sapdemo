_schema-version: '3.1'
ID: RiskMngt
version: 1.0.0
description: A simple CAP project.
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
    - builder: custom
      commands:
        - rm -rf build/dist
        - mkdir -p build/dist
        - rm -rf build/ui-deployer
        - mkdir -p build/ui-deployer
    - builder: custom
      commands:
        - npm install --production
        - >-
          bash -c "cds -v | grep 'cds-dk' || npm i --production --no-save
          @sap/cds-dk @sap/mdk-tools"
        - bash -c "export PATH=$PATH:$(pwd)/node_modules/.bin"
        - cds build --production
  after-all:
    - builder: custom
      commands:
        - npm install
modules:
  - name: RiskMngt-RiskMngt.Risks
    type: html5
    path: app/RiskMngt.Risks
    build-parameters:
      builder: custom
      commands:
        - rm -rf dist-zip dist
        - >-
          npx -p @ui5/cli@^3.0.0 ui5 build --dest dist
          --include-task=generateManifestBundle
          --include-task=generateCachebusterInfo
        - mkdir dist-zip
        - bash -c "cd dist && npx bestzip ../dist-zip/RiskMngtRisks.zip *"
      _artifactName: RiskMngtRisks.zip
      supported-platforms: []
      build-result: dist-zip
  - name: RiskMngt-db-deployer
    type: hdb
    path: gen/db
    parameters:
      buildpack: nodejs_buildpack
    build-parameters:
      builder: npm
      ignore:
        - node_modules
        - package-lock.json
    requires:
      - name: RiskMngt-service-uaa
      - name: RiskMngt-service-db
  - name: RiskMngt-srv
    type: nodejs
    path: gen/srv
    parameters:
      memory: 256MB
    provides:
      - name: srv-api
        properties:
          srv-url: ${default-url}
    build-parameters:
      builder: npm
      ignore:
        - node_modules
        - package-lock.json
    requires:
      - name: RiskMngt-service-uaa
      - name: RiskMngt-service-db
      - name: RiskMngt-service-destination
      - name: RiskMngt-service-metadata
      - name: RiskMngt-service-connectivity
      - name: RiskMngt-service-logs
  - name: RiskMngt-aux-destinations
    type: com.sap.application.content
    parameters:
      content:
        subaccount:
          existing_destinations_policy: update
          destinations:
            - Authentication: OAuth2UserTokenExchange
              Name: RiskMngt-service-uaa
              ServiceInstanceName: RiskMngt-uaa
              ServiceKeyName: RiskMngt-uaa-key
              sap.cloud.service: lcap.RiskMngt
            - Authentication: OAuth2UserTokenExchange
              Name: RiskMngt-app-srv
              TokenServiceInstanceName: RiskMngt-uaa
              TokenServiceKeyName: RiskMngt-uaa-key
              URL: ~{srv-api/srv-url}
              sap.cloud.service: lcap.RiskMngt
            - Name: RiskMngt-service-html5-apps-repo-host
              ServiceInstanceName: RiskMngt-html5-apps-repo-host
              ServiceKeyName: RiskMngt-html5-apps-repo-host-key
              sap.cloud.service: lcap.RiskMngt
            - Authentication: OAuth2UserTokenExchange
              Name: RiskMngt-RiskMngtService
              TokenServiceInstanceName: RiskMngt-uaa
              TokenServiceKeyName: RiskMngt-uaa-key
              URL: ~{srv-api/srv-url}/service/RiskMngt
              sap.cloud.service: lcap.RiskMngt
              sap.applicationdevelopment.actions.enabled: false
              sap.build.usage: CAP
              sap.processautomation.enabled: false
              HTML5.DynamicDestination: false
              WebIDEEnabled: false
              MobileEnabled: false
              AppgyverEnabled: false
    build-parameters:
      no-source: true
    requires:
      - name: RiskMngt-service-uaa
        parameters:
          service-key:
            name: RiskMngt-uaa-key
      - name: srv-api
      - name: RiskMngt-service-html5-apps-repo-host
        parameters:
          service-key:
            name: RiskMngt-html5-apps-repo-host-key
      - name: RiskMngt-service-destination
        parameters:
          content-target: true
  - name: RiskMngt-launchpad
    type: html5
    path: build/launchpad
    build-parameters:
      builder: custom
      commands:
        - npm install
        - >-
          npx -p @ui5/cli@^3.0.0 ui5 build --dest dist
          --include-task=generateManifestBundle
          --include-task=generateCachebusterInfo
        - bash -c "cd dist && npx bestzip ../../../build/dist/launchpad.zip *"
        - rm -rf dist
      _artifactName: launchpad.zip
      supported-platforms: []
      build-result: ../../build/dist
  - name: RiskMngt-aux-ui-deployer
    type: com.sap.application.content
    path: build/ui-deployer
    build-parameters:
      build-result: resources
      requires:
        - name: RiskMngt-RiskMngt.Risks
          artifacts:
            - RiskMngtRisks.zip
          target-path: resources
        - name: RiskMngt-launchpad
          artifacts:
            - launchpad.zip
          target-path: resources
    requires:
      - name: RiskMngt-service-html5-apps-repo-host
        parameters:
          content-target: true
resources:
  - type: org.cloudfoundry.managed-service
    name: RiskMngt-service-uaa
    parameters:
      service: xsuaa
      service-plan: application
      service-name: RiskMngt-uaa
      path: xs-security.json
      config:
        oauth2-configuration:
          redirect-uris:
            - https://*.${default-domain}/login/*
        xsappname: RiskMngt-${space}
        role-collections:
          - description: ''
            name: RiskMngt-RiskViewer-${space}
            role-template-references:
              - $XSAPPNAME.RiskViewer
          - description: RiskManager
            name: RiskMngt-RiskManager-${space}
            role-template-references:
              - $XSAPPNAME.RiskManager
  - type: org.cloudfoundry.managed-service
    name: RiskMngt-service-db
    parameters:
      service: hana
      service-plan: hdi-shared
      service-name: RiskMngt-db
  - type: org.cloudfoundry.managed-service
    name: RiskMngt-service-destination
    parameters:
      service: destination
      service-plan: lite
      service-name: RiskMngt-destination
      config:
        HTML5Runtime_enabled: true
        version: 1.0.0
  - type: org.cloudfoundry.user-provided-service
    name: RiskMngt-service-metadata
    parameters:
      service: metadata
      service-plan: standard
      service-name: RiskMngt-metadata
      config:
        default-domain: ${default-domain}
  - type: org.cloudfoundry.managed-service
    name: RiskMngt-service-connectivity
    parameters:
      service: connectivity
      service-plan: lite
      service-name: RiskMngt-connectivity
  - type: org.cloudfoundry.managed-service
    name: RiskMngt-service-logs
    parameters:
      service: application-logs
      service-plan: lite
      service-name: RiskMngt-logs
  - type: org.cloudfoundry.managed-service
    name: RiskMngt-service-html5-apps-repo-host
    parameters:
      service: html5-apps-repo
      service-plan: app-host
      service-name: RiskMngt-html5-apps-repo-host
